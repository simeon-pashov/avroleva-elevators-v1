/** reporting (L4) - dashboard, monthly PDF, exports (read-only; may SELECT any table). STEP 2 adds the dashboard queries. */
export const moduleInfo = { name: 'reporting', layer: 4, status: 'skeleton' } as const
