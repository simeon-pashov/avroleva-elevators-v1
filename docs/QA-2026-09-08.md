# QA pass — 2026-09-08 (step 6, before the morning report)

Environment: Windows 11, Node 24, Chrome 152 (driven with playwright-core, headless), Postgres 16 in
`unclecrm-db`, `npm run db:reset` then `npm run dev` (API :3005, office :5175, tech :5176).
Method: every office route crawled in Bulgarian and in English at 1280 px and in Bulgarian at
820 px (console errors, failed responses ≥ 400, raw i18n keys on screen, horizontal overflow,
screenshot per page), then scripted end-to-end flows through the real UI, role crawls as
`maria`/`ivan`, an API role matrix, the admin isolation flow, and the technician app online →
offline → online. Every failure below was reproduced, fixed, and re-run green. Screenshots of the
final state are in `docs/screenshots/`.

## 1. Bugs found and fixed

| # | Severity | Where | Symptom | Fix (commit) |
|---|---|---|---|---|
| 1 | high | API `billing.rollOverdue` | Concurrent billing reads (dashboard + payments widget + summary load at once) each rolled the same issued invoices and emitted `InvoiceOverdue` 3–5× per invoice; the demo bell showed **99+** minutes after `db:reset`. | One atomic `UPDATE … RETURNING`; 4 concurrent rolls now emit exactly n events (`step6.test.ts`). `29b2216` |
| 2 | high | seed + outbox sweep | The seed writes months of history through the real services; `events.sweep()` found those events without delivery rows and delivered them all on the first API start: 144 in-app + 54 Viber + 18 e-mail "issued invoice" rows plus the duplicates from #1 (266 rows). | `events.acknowledge(tenantId, SUBSCRIPTIONS)` at the end of the seed marks history delivered without running handlers; the demo inbox now holds the 6 curated rows only. `29b2216` |
| 3 | high (deploy) | API `static.ts` | Under the production image `GET /tech/` looped: Express 5 matched `app.get('/tech')` for `/tech/` too and redirected it to itself. The PWA was unreachable in the container. | Exact-match regex. `f7f5b15` |
| 4 | high (deploy) | office `ElevatorTabs`, tech `http.ts` | Behind the `/avroleva/` prefix photo thumbnails and downloads requested `/avroleva/avroleva/files/…` (the API already mints BASE_PATH into signed URLs). | `serverPath()` in the office, no double prefix in the tech client. `f7f5b15` |
| 5 | medium | office nav | At 1280 px the top nav was clipped after "Договор…" (Отчети, Уведомления, Импорт, Потребители, Настройки reachable only by an invisible horizontal scroll); at 820 px only four items were visible. | Nav wraps; topbar `min-height`. `6854814` |
| 6 | medium | office, technician role | `ivan` saw contract prices on the contracts list and detail, building and customer pages (the dashboard widgets and elevator panel were already gated). | Every money cell gated to owner/office. Verified with a role crawl. `6854814` |
| 7 | medium | office | No way to generate the month's invoices from the UI (API only). | "Генерирай фактури" button in the payments widget (confirm, idempotent, toast with created/skipped). `6854814` |
| 8 | low | notifications log | The link column showed the raw `relatedType` ("invoice"). | Translated "Отвори"/"Open". `6854814` |
| 9 | low | office, tech, print/public pages | No favicon → a 404 in the console on every page load. | Inline SVG favicon (office), PNG icon (tech), `data:` icon on server-rendered pages. `6854814` |
| 10 | low | tech typecheck | `apps/tech/src/pages/VisitPage.tsx` did not compile against the step-5 contract (`photosPurgedAt`). | Field added. `c00cf37` |
| 11 | low | `.gitignore` | `.env.production.example` was ignored by the `.env.*` rule. | Exception added. `8d75828` |

Not bugs, but noted: the office at 820 px is usable but dense (tables scroll inside their
wrappers, no page overflow); the login page has no "forgot password" (by design — the owner
resets from Потребители, the platform admin from /admin); the `PUBLIC_BASE_URL` documented in
ARCHITECTURE §7 (`…/avroleva`) is wrong for the code, which appends `BASE_PATH` itself — the
deploy files use the origin only (see `DEPLOY.md`).

## 2. What was exercised (all green after the fixes)

**Crawl** (`demo`, owner): 28 routes × bg/en at 1280, 18 routes at 820, 3 admin routes. Zero
console errors apart from the expected pre-login `/auth/me` 401, zero failed API calls, zero raw
i18n keys, no horizontal page overflow. The English pass shows no untranslated strings.

**Flows through the UI** (`demo`):
- Record a visit from the dashboard due list (dialog: date, kind, technicians checkboxes, notes) and from the elevator page (kind = техническо обслужване) — both appear in the history with the technicians.
- Callback: Нова авария → pick elevator by search → повреда, caller, phone → Изпрати техник (inline select) → На място → Приключи (cause, action, "record a visit" checked) → status `closed`, response time computed, the close-out visit exists.
- Stop-lift defect (catalogue item 1, висока, "Асансьорът трябва да спре") on an elevator that was not stopped: the map legend went 2 → 3 "спрян" (ring), Отстранен → back to 2.
- Inspection: Нов преглед (periodic, body, scheduled 2026-10-15) → appears in the calendar and the API.
- Invoices: "Генерирай фактури" for 2026-10 → 12 issued, 0 skipped (re-run: 0 issued, 12 skipped); "Отбележи като платено" on one → payment recorded, totals updated.
- Exports: elevators CSV (UTF-8 BOM, 21 lines), full export (zip, 350 KB, signed link 200, tampered/expired 404).
- Print pages: logbook, defect notice, inspection request, single label, building label sheet, monthly building report — all 200 and readable (Cyrillic, QR codes).
- Public page `/p/:token`: loads, the fault form creates a callback with `channel=public_page`; a 6th report within the hour → 429.

**Roles**: API matrix for `ivan` (technician) — 403 on billing, users, sessions, exports (list, CSV, full), reports, notifications log/rules, tenant patch, delete request, imports, invoice generation, building-report print; 200 on inbox, tenant, dashboard, visits, callbacks, logbook print. `maria` (office) — 200 on money/exports/reports/notifications/users list, 403 on user creation, sessions, tenant patch, delete request. UI crawl: `ivan` is bounced to `/` from /reports, /notifications, /import, /users, /settings/notifications, /settings/data and sees no payments widget, no prices, no CSV buttons; `maria` is bounced from /users only.

**Admin / isolation**: registered "QA Лифт" with an owner from /admin/tenants/new, logged in as that owner in a second browser context: every list empty (users = 1), dashboard 0/0; the demo tenant's elevator, building, visit, defect, inspection, report, logbook and building-report URLs → 404. Деактивирай → the owner's session answers 401 and login is refused; reactivate → login 200.

**Technician app**: enrolled `ivan` with a token from Потребители → "QA телефон" → Today list pulled (clock offset 0 s). Visit with checklist "Всичко OK", second technician, one photo, notes → outbox drained → office shows `source=app`, `timestampSource=device`, 21 checklist items, photo uploaded, thumbnail 200. API process killed → second visit with a photo recorded from the cached Today list → outbox "2 записа чакат, 1 снимка за качване" → API restarted → drained within 30 s → both visits in the office with photos.

**Security quick-check**: login limiter 20/15 min per IP (21st → 429); public fault form 5/h per elevator (6th → 429); signed file URLs: fresh 200, `exp` in the past 404, tampered `sig` 404, no signature 404; `git ls-files | grep .env` → `.env.example`, `.env.production.example` only; no literal secrets in tracked code; cross-tenant 404s covered by `step2–5` tests (callbacks, defects, inspections, visits, attachments, sync, exports, reports, notifications) and by the admin flow above.

## 3. Test / lint / build after the pass

`npm run typecheck` green; `npm run lint` (eslint + prettier) green; `npm test`: i18n 12 + API 203 = **215** (was 212; `step6.test.ts` adds 3); `npm run build` (packages, api, office, tech + PWA) green. Docker image built and verified locally behind a throwaway nginx (see `DEPLOY.md` and the `8d75828` commit message).

## 4. Left as known gaps (not fixed here)

- The demo tenant keeps growing while you click around; `npm run db:reset` restores it (done at the end of this pass).
- Service-worker registration was not verifiable inside the embedded browser pane during the Docker run; `sw.js` serves correctly — confirm the PWA install on a real phone after deploy.
- No Playwright suite is committed; the scripts used here lived in a scratch folder (they log in through the UI, crawl, and drive the dialogs). Worth porting into `apps/office/e2e/` before the first customer.
