/** notifications (L4) - templates, rules, channel adapters (console adapters live in platform/adapters). Phase 7. */
export const moduleInfo = { name: 'notifications', layer: 4, status: 'skeleton' } as const
